# Configure GPIO Tasks and Events (GPIOTE)

- Connect pin to channel
- Define transition to listen to
- Enable interrupt

```rust,noplaypen
{{#rustdoc_include ../../../../src/bin/interrupt_example.rs:init_gpiote}}
```
